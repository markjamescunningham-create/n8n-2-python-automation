# Your OpenAI API key (platform.openai.com → API keys)
OPENAI_API_KEY=sk-...

# Model to use. gpt-4o-mini is fast and cheap; use gpt-4o for higher quality
OPENAI_MODEL=gpt-4o-mini

# Directory to save Markdown summaries
OUTPUT_DIR=./summaries

# Port for API server mode (--serve flag)
PORT=5001
